{
  "name": "tas-kag-t-makas-oyunu-yap",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless tas-kagit-makas oyunu — Express statik sunucu + istemci Math.random()",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
